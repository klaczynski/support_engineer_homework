<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Cancelled</title>
    <link href="/css/styles.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <h1>All Accounts</h1>
    <table>
      <tr>
        <th>Organization Name</th>
        <th>Created Date</th>
        <th>Plan Name</th>
        <th>Status</th>
      </tr>
      <% accts.forEach((acct)=> { %>
      <tr>
        <td><%= acct.orgname %></td>
        <td><%= moment(acct.createddate).format('DD/MM/YYYY') %></td>
        <td><%= acct.planname %></td>
        <td><%= acct.status %></td>
      </tr>
      <% }) %>
    </table>
  </body>
</html>
